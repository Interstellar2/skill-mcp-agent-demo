# Kitchen Agent 配置文件
# 复制本文件为 .env 并填入实际值

# OpenAI 兼容 API 配置（必填，用于 Agent 模式）
OPENAI_API_KEY=your-api-key-here

# API Base URL（可选，使用第三方兼容服务时填写，如阿里云、DeepSeek 等）
# OPENAI_BASE_URL=https://api.openai.com/v1

# 默认模型（可选，默认 gpt-4o-mini）
# MODEL=gpt-4o-mini
